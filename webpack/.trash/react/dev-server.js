export default (options) => ({
	devServer: {
		clientLogLevel: 'info',
		// compress: true,
		// contentBase: options.contentBase,
		// hot: true,
		// lazy: true,
		// poll: true,
		port: 80,
		// publicPath: '/',
		// stats: 'errors-only',
	},
})
